[project]
name = "pydantic-ai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic-ai-slim[bedrock]>=0.0.41",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
